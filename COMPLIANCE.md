# 合规性审计报告 — B站首页分类脚本开源与商业化

审计对象：`bilibili-feed-mode.user.js` v0.9（及可能一同发布的周边文件）
审计日期：2026-08-25
审计场景：A）非营利开源发布（GitHub + Greasy Fork / 浏览器扩展商店）；B）存在盈利或成本回收（预置 key、收费）

> 本报告为技术侧合规梳理，不构成法律意见。涉及实际商业化决策前建议咨询执业律师，尤其是中国《反不正当竞争法》互联网条款相关判例。

---

## 0. 发布前必须处理的硬伤（无论哪种场景）

| # | 问题 | 处理 |
|---|---|---|
| 1 | **DeepSeek API key 硬编码在脚本第 16 行** | 改为设置界面输入、存 localStorage；发布前从代码和 git 历史中彻底移除。该 key 已在开发过程中暴露过，无论如何都应去 DeepSeek 后台**作废换新** |
| 2 | 脚本会把视频标题/UP主名/标签发送给第三方（DeepSeek） | README 和脚本描述中必须明示（见 §2.3 隐私），Greasy Fork 有强制披露规则 |
| 3 | `browser_server.py` 含本机路径、个人配置文件路径 | 不建议随脚本发布；如发布需参数化并清理 |
| 4 | `violentmonkey.xpi`、`shots/` 截图（含你的账号头像/浏览记录） | 加入 `.gitignore`，绝不入库 |
| 5 | 名称含"B站" | 保留描述性用法可以，但需加"非官方工具，与哔哩哔哩无关联"免责声明，不得使用 B站 logo/商标素材 |

---

## 1. 现状盘点：发布物里有什么

- 读取 B 站**非公开 Web API**（首页推荐流 rcmd、标签、分区 feed、综合热门），以用户自己的登录态请求，量级与正常浏览同数量级（有全局节流与退避）。
- **修改 B 站首页 DOM**：过滤模式下隐藏原生信息流、注入自渲染卡片。
- **隐藏 B 站广告卡片**（`cm.bilibili.com` 商业推广卡在过滤模式下被隐藏）⚠️ 这是全篇风险最高的单点，见 §2.2 与 §3.2。
- 调用 DeepSeek API 做内容分类（外发数据：视频标题、UP主名、标签；不含 cookie、不含用户身份）。
- 本地存储：分类缓存、模式偏好、缓冲池（localStorage/sessionStorage），无远端上报。

---

## 2. 场景 A：非营利开源

### 2.1 平台协议（B站用户协议）

- B 站用户协议禁止"未经授权使用插件、外挂或第三方工具干扰服务"以及未经许可抓取数据。**个人使用+开源免费分发处于灰色地带**：同类项目（bilibili-evolved、BewlyBetter 等，均为万星级项目）长期存在，B 站对非营利社区工具历史上采取容忍态度，但协议上并无豁免。
- 对**使用者**的现实风险是账号层面（风控、限流、极端情况封号）——README 必须向用户披露"使用可能违反 B 站用户协议、存在账号风险，风险自担"。
- 对**发布者**的风险在非营利场景下很低：中国《反不正当竞争法》原则上规制"经营者"行为，无经营行为则缺少构成要件；著作权侵权不成立（未复制其代码/素材，DOM 修改发生在用户本地浏览器）。

### 2.2 广告隐藏（本项目最大法律敏感点）

- 中国法院在多起判例中认定**商业性**广告屏蔽构成不正当竞争（优酷诉金山猎豹浏览器案、爱奇艺相关广告过滤案、腾讯诉世界之窗浏览器案等），"技术中立""用户选择"抗辩在商业场景基本都败诉。
- **非营利场景下无经营者身份，被诉风险显著更低**（个人开源作者被判不正当竞争的先例罕见），但并非零。
- 缓释建议（两种场景都适用）：把"隐藏广告卡"做成**默认关闭的独立开关**，或干脆改为"广告卡不参与分类、原样显示"。本项目核心价值是分类而非去广告，砍掉这个功能损失极小、消掉的风险极大。**强烈建议发布版移除广告隐藏**。

### 2.3 隐私与个人信息保护（PIPL）

- 外发数据（标题/UP名/标签）本身是平台公开数据，但**它们组合起来反映了用户的推荐流=兴趣画像**，谨慎起见按"可能涉及个人信息"处理：
  - README + Greasy Fork 描述中写明：发送什么、发给谁（DeepSeek 或用户自选服务商）、不发送什么（cookie、账号身份、观看历史）、DeepSeek 按其隐私政策处理。
  - 脚本首次启用 LLM 分类时给一次性提示确认（一行 confirm 即可），满足知情同意。
- 无自建服务器、无数据收集 → 无需隐私政策全文、无备案义务。这是非营利 BYOK 架构最大的合规红利。

### 2.4 商店/平台政策

- **Greasy Fork**：要求源码可读（禁混淆）✅、不得未披露地外发数据（→按 §2.3 披露）、含收费/广告/追踪需标注 antifeature（非营利版无）。当前代码合规。
- **Firefox AMO / Chrome Web Store**（若改造成扩展）：需申报数据用途（发送内容数据给第三方 API）；内容修改类扩展（广告拦截器同类）政策上允许；禁止误导性命名（不得暗示官方）。注意 CWS 要求扩展内远程执行代码受限——本脚本只 fetch 数据不 eval 远程代码 ✅。
- **GitHub**：无特殊障碍；勿传 key、截图、xpi。

### 2.5 开源许可证

- 无第三方代码依赖（纯手写 vanilla JS），无传染性义务，许可证可自由选择。
- **如果未来可能商业化，建议 GPLv3**：防止他人闭源套壳拿去卖，你作为唯一版权人保留双重许可（对外 GPL、对自己/付费版任意）的权利。纯利他场景选 MIT 亦可。
- 需要 `LICENSE` 文件 + 脚本头部许可证声明。

### 2.6 非营利场景结论

| 维度 | 风险 | 备注 |
|---|---|---|
| B站协议/数据抓取 | 🟡 低-中 | 用户账号风险需披露；发布者风险低 |
| 广告隐藏 | 🟡 中 | 建议直接移除该功能 → 🟢 |
| 隐私/PIPL | 🟢 低 | 做好披露即可 |
| 商店政策 | 🟢 低 | 按清单整改即合规 |
| 许可证/商标 | 🟢 低 | 加 LICENSE + 免责声明 |

**整改后可以发布。**

---

## 3. 场景 B：盈利 / 成本回收

盈利形态按风险从低到高分三档，**每往上一档，义务和风险都是叠加的**：

### 3.1 第一档：捐赠（GitHub Sponsors / 爱发电）🟢

- 软件本体免费开源，接受自愿捐赠。法律上一般不构成"经营行为"，不改变场景 A 的风险面。
- 义务：个人所得税申报义务理论上存在（偶然所得/劳务报酬），金额小实务中常被忽略，但应知悉。
- **这是"不亏本"诉求下的首选方案。**

### 3.2 第二档：收费但 BYOK（卖软件/卖增值功能，用户自带 key）🟡

- 一旦收费，你成为**经营者**，《反不正当竞争法》完全适用：
  - **广告隐藏功能在此档必须移除**——商业产品+屏蔽平台广告=判例中最成熟的败诉模型，没有侥幸空间。
  - "接管信息流、替换推荐"在商业场景也可能被主张为干扰经营/流量劫持。缓释：保留"全部"模式为默认、强调用户主动开启、不劫持任何跳转和收益（卡片仍链向 B 站原视频页 ✅ 本项目现状如此）。即便如此，**向 B 站主张的商业化工具收费本身就是被诉风险的开关**，需自行权衡或获得平台合作授权（B站有开放平台，但未开放此类接口）。
- 经营主体义务：办理个体工商户/公司、纳税、开票；纯软件售卖走 App/脚本平台或海外渠道（Gumroad 等）可简化，但税务义务不变。
- 商店侧：Greasy Fork 需标注 antifeature（付费功能）；CWS 已取消内置付费，需自建授权验证。

### 3.3 第三档：预置 key / 托管代理（你出 key，用户订阅付费）🔴 义务最重

**"把 key 预制在客户端里"直接排除**——技术与协议双重不可行：

1. 客户端内的 key 等于公开：任何人可提取盗刷，账单风险全在你。
2. DeepSeek 开放平台协议禁止转让/出借 key、要求使用者对 key 负全责；把共享 key 分发给不特定公众属于违约，可被封号。

**唯一正确形态是服务端代理**：用户请求 → 你的服务器（持 key、鉴权、计量、限额）→ DeepSeek。但这让你从"软件作者"变成"网络服务提供者"，新增一整层义务：

| 义务 | 说明 |
|---|---|
| ICP 备案 | 境内服务器提供 web 服务必须备案；**收费服务**还涉及经营性 ICP/EDI 许可（增值电信业务），个人无法办理，需公司主体 |
| PIPL 完整义务 | 你开始处理用户数据（请求内容、账号、支付信息）：需要隐私政策、同意机制、安全措施、数据出境评估（如服务器/模型在境外） |
| 生成式 AI 监管 | 《生成式人工智能服务管理暂行办法》《深度合成规定》规制"向境内公众提供生成式 AI 服务"。分类标签场景属于边缘形态，但作为持 key 的服务提供方，备案/评估义务的适用性需要专业判断 |
| 上游协议 | 使用 API 输出提供对外服务需符合 DeepSeek 商用条款；确认其对转售/托管场景的授权口径 |
| 税务/主体 | 同 3.2，且订阅制收入必然要走正规主体 |
| 不正当竞争 | 同 3.2，且"持续性经营收入"会加重被诉时的赔偿计算 |

### 3.4 盈利场景结论与推荐路线

```
推荐路线：
  默认形态 = 开源 + BYOK + 免费 + 移除广告隐藏 + 捐赠入口   ← 场景A + 3.1，风险最低
  进阶形态 = 上述基础上出「托管订阅版」：
      前提：注册经营主体、ICP/EDI、隐私政策、DeepSeek 商用授权确认
      形态：服务端代理 key + 账号体系 + 限额；开源版功能不阉割（社区信誉）
  不要做：客户端预置 key（任何档位都不要）
         付费版保留广告隐藏（判例级风险）
```

---

## 4.5 假想场景审计：内嵌浏览器 / 自建浏览器

> 审计问题：本项目的功能（信息流分类过滤、接管注入、热榜混入）能否作为**内嵌浏览器（WebView 套壳 App）**或**自建浏览器（内置功能的浏览器产品）**的一部分对外提供？

### 形态一：内嵌浏览器（WebView 套壳 App，核心卖点=改造后的B站/YouTube）🔴 不可行

法律与技术双重阻断：

1. **中国判例直接命中**：视频聚合类 App（"电视猫"等深度链接/嵌套播放案）即使不碰广告也被判不正当竞争——"把他人服务嵌进自己产品并改变呈现"本身就是被规制的行为模式。本项目在接管模式下**隐藏原生流、用自渲染卡片替换**，在套壳 App 语境里会被定性为擅自改造平台服务，比单纯聚合更进一步。
2. **《互联网信息服务算法推荐管理规定》**：对信息内容做算法化筛选与重排、面向公众提供，即构成算法推荐服务提供者，触发备案、安全评估等义务；套壳 App 作为具有舆论属性的信息服务还涉及更多资质。
3. **平台协议**：B站、YouTube 用户协议均禁止通过非官方客户端访问服务。YouTube ToS 明文禁止"通过修改后的客户端访问"；且 **Google 在技术上封锁 WebView 内登录**（disallowed_useragent），个性化推荐流在套壳里根本拿不到——法律未动，技术先死。
4. **应用商店**：Google Play / App Store / 国内商店对"套壳+修改第三方服务"类 App 的审核基本不予通过。

### 形态二：自建浏览器（通用浏览器，内置此功能）🟡→🔴（商业化即红）

- **中国判例映射最直接**：优酷诉金山（猎豹浏览器）、腾讯诉世界之窗——都是"通用浏览器内置针对特定网站的改造功能"败诉。虽然那些案子的功能是广告过滤而本项目刻意保留广告，但反法第 12 条"干扰他人网络产品正常运行"的兜底条款并不限于广告：**隐藏原生推荐流、替换为自渲染卡片、后台调用非公开接口预取**，每一项都可能被主张为"干扰"。浏览器作为商业产品（即使免费，浏览器发行本身通常被认定为经营行为），"用户选择""技术中立"抗辩在此类案件中胜率极低。
- **境外相对友好但有边界**：德国联邦最高法院在 Axel Springer 诉 Eyeo（AdBlock Plus）中确认用户端内容过滤合法，欧盟环境下"用户自主的呈现控制"有判例支撑；美国侧 hiQ/Van Buren 之后公开内容访问的 CFAA 风险低，主要剩合同（ToS）与商店政策风险。但注意这些友好判例保护的是**用户自主安装的过滤工具**，浏览器厂商"内置并默认开启"会显著削弱该抗辩。
- **若坚持要做，风险降到最低的形态**：功能默认关闭、首次使用需用户明示开启（把"用户自主"做实）；去掉接管注入，退回纯隐藏过滤（不替换平台呈现）；去掉调用非公开接口的预取（B站缓冲池、热榜混入必须砍掉）；广告豁免保持；不预置 key。即便如此，在中国大陆商业发行仍属高风险，需正式法律意见。

### 形态三（对照组）：现行形态——用户自装脚本/扩展 🟢 风险最低

"用户自己决定在自己的浏览器里装什么"是整个合规结构中最强的支点。**任何把安装决定从用户手里挪到发行者手里的形态变化（内置、预装、套壳），都是在拆这个支点。**若要做浏览器，最稳的路线是：做一个支持扩展生态的通用浏览器，让用户自行从商店安装本扩展——发行者与改造行为之间保持用户决策这道防火墙。

### 结论速览

| 形态 | 中国大陆 | 境外 | 技术可行性 |
|---|---|---|---|
| 用户自装脚本/扩展（现状） | 🟢 | 🟢 | ✅ |
| 通用浏览器+用户自装扩展 | 🟢~🟡 | 🟢 | ✅ |
| 自建浏览器内置（默认关） | 🟡 | 🟡 | ✅ |
| 自建浏览器内置（默认开/商业化） | 🔴 | 🟡 | ✅ |
| WebView 套壳 App | 🔴 | 🔴 | ❌（YouTube 登录被封锁） |

### 4.5.1 追问场景：默认无功能 + 免责条款 + 用户一键打包安装内嵌浏览器与插件（非营利）

结论：**免责条款 + 一键按钮不能重建"用户自装"防火墙；非营利降低的是被执行的概率，不是行为定性。**

- **实质重于形式**：防火墙的本质是三个分离——通用工具、用户自选生态、多次独立决策。一键打包把浏览器、插件、组装方式全部变成发行者设计的一体化方案，实质 ≈ 内置功能+一个确认框。判例（猎豹案）已明确穿透"用户自愿开启"抗辩：法院看谁架构了改造，不看谁点了按钮。
- **免责条款效力边界**：只约束发行者与用户之间的合同关系，对平台（非当事人）的不正当竞争/侵权主张无任何对抗力。
- **非营利的真实作用**："经营者"要件存疑 + 索赔动力低 → 现实风险从诉讼降为函告/技术反制（封号、封 UA、封分发）。定性不变。
- **技术死结不因形态变化解除**：Google 封锁 WebView/Electron 内登录，YouTube 个性化流拿不到；UA 伪装属进一步违反政策。
- **形态梯度**：①说明书式（🟢）→ ②引导安装器：按钮拉起各官方渠道页、用户逐步在官方界面确认（🟢~🟡，**推荐**）→ ③一键打包捆绑（🟡非营利/🔴沾商业）→ ④直接内置（🔴）。第②档以两三次点击的代价保留完整决策链，是便利性与合规性的最优交点。

### 4.5.2 追问场景：产品收集分析用户浏览数据并构建个人模型（纯端侧、无登录、无服务器）🟢

前提：用户自己的浏览器 + 用户自装扩展；浏览数据与个人模型全部存储、分析于用户设备本地；无账号体系、无服务器、无遥测。

结论：**这是全部审计场景中唯一的满盘绿。**架构本身使开发者无法接触任何用户数据——法律上不构成个人信息处理者，PIPL/GDPR 的处理者义务链条不启动；无登录意味着连账号数据、服务器日志这两个最常见的义务来源都不存在。"用户完全掌控"不再依赖条款承诺，而是可验证的架构事实（配合开源可审计）。

残留注意项（小但要做）：

1. **BYOK 的 LLM 调用是唯一出设备的数据通道**：若个人模型的构建管线把浏览记录衍生文本发给 LLM 服务商，即使走用户自己的 key，也必须在界面上明示，并提供纯本地模式（本地规则/本地小模型）作为选项。BYOK 的法律意义在于：与 LLM 服务商建立关系的是用户本人，开发者始终不在数据链路里。
2. **供应链诚实**：扩展的自动更新等于向用户设备推送可执行代码——"永不添加遥测"要写成公开承诺，并通过官方商店分发更新（连更新基础设施都不自建），开源仓库作为可审计凭证。
3. **披露义务不因"非处理者"而免除**：Greasy Fork/扩展商店的数据披露规则按平台政策照常适用；写一份简短隐私说明（什么留在本地、什么经 BYOK 出去、除此之外没有了）。
4. 平台侧维持原判：读取"用户看到的内容"而非批量爬取历史接口，用户数据可携带权（PIPL 45条、hiQ 案）与自装防火墙叠加，灰色偏白。
5. 声明产品面向成年人，未成年人监护同意的负担不附着于开发者。

## 4. 发布前整改清单（Checklist）

项目已选定路线：**开源 + BYOK + 免费 + 捐赠**（场景 A + §3.1），并采取"不损害平台权益"的强化姿态。

- [x] 移除硬编码 key → 改为 ⚙ 设置输入（存 localStorage）；**现有 key 需用户自行去 DeepSeek 后台作废换新**
- [x] 广告处理超出原建议：不止移除隐藏，而是**主动豁免所有商业内容**——cm 推广卡与课堂（自营付费推广位）在任何模式下原样显示；番剧/影视等官方推广楼层不再被接管一刀切隐藏，恢复按分类展示
- [x] 数据外发告知：内置于 ⚙ 设置弹窗（输入 key 前必读），并写入 `@description`、README
- [x] README：功能 / 隐私披露 / 非官方免责声明 / 账号风险提示 / 安装方式 / 对B站的态度声明
- [x] LICENSE：GPL-3.0（版权人保留双许可空间）
- [x] `.gitignore`：排除 xpi、截图、本地自动化工具（browser_server.py）
- [x] 脚本 `@description` 同步披露；`@license GPL-3.0`
- [x] git 仓库从干净历史开始（v1.0.0 起，不含任何带 key 的历史版本）
- [ ] （若未来商业化）咨询律师：反不正当竞争评估 + DeepSeek 商用条款确认
